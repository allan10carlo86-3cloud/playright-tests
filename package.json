{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:maybe-report": "powershell -NoProfile -Command \"npx playwright test --reporter=html; if ($LASTEXITCODE -ne 0) { npx playwright show-report }\"",
    "test:with-paths": "cross-env NODE_OPTIONS=\"-r ts-node/register -r tsconfig-paths/register\" npx playwright test --reporter=html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "cross-env": "^7.0.3"
  }
}
